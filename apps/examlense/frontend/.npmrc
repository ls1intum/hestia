# react-day-picker@8 declares a peer dep of date-fns@^2||^3, but the project uses
# date-fns@4. The APIs it consumes are compatible; accept the mismatch so `npm ci`
# (strict about peer deps in CI/Docker) resolves the same tree local dev already uses.
legacy-peer-deps=true
